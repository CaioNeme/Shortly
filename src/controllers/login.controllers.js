import { db } from "../database/database.connetcion.js";

export async function signUp(req, res) {
  try {
  } catch (err) {
    res.status(500).send(err.message);
  }
}

export async function signIn(req, res) {
  try {
  } catch (err) {
    res.status(500).send(err.message);
  }
}

export async function usersMe(req, res) {
  try {
  } catch (err) {
    res.status(500).send(err.message);
  }
}
